# Bike Domain

Pure domain types/rules, independent from Nuxt/Fastify.

Start with:

```text
StandardCode
BikeSpec
CandidateSpec
CompatibilityRule
CompatibilityCheck
CompatibilityResult
evaluateCompatibility()
```
